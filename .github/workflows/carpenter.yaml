name: Carpenter
on:
  push:
    branches:
      - "**"
  release:
    types:
      - published
jobs:
  carpenter:
    uses: jdowni000/arcaflow-container-toolkit/.github/workflows/reusable_workflow.yaml@dev
    with:
      image_name: ${{ github.event.repository.name }}
      image_tag: 'latest'
    secrets: inherit     
